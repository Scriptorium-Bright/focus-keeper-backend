spring:
  application:
    name: adhd-focus-mate

  datasource:
    url: jdbc:postgresql://localhost:5432/focusmate
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    defer-datasource-initialization: true
    open-in-view: false  # OSIV 비활성화: API 서버 성능 최적화
    properties:
      hibernate:
        format_sql: true
        # dialect 삭제: Hibernate 6+에서는 자동 감지됨

  sql:
    init:
      mode: always
      continue-on-error: true

  data:
    redis:
      host: localhost
      port: 6380

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: focusmate-group
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: "*"

server:
  port: 8081

logging:
  level:
    org.springframework: INFO
    com.adhd.focusmate: DEBUG
    # Kafka 재시도 로그 확인용
    org.springframework.kafka.listener: DEBUG
    org.springframework.kafka.retrytopic: DEBUG

# Actuator & Prometheus
management:
  endpoints:
    web:
      exposure:
        include: health, info, prometheus, metrics
  endpoint:
    health:
      show-details: always
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}
